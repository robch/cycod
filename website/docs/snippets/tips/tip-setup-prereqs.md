??? tip "If you don't have the `cycod` CLI installed ..."

    Before you begin, you'll need to install the `cycod` CLI.

    [:octicons-arrow-right-24: Install the `cycod` CLI](/install-cycod-cli.md)  
