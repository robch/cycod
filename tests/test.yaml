
- name: help
  command: chatx
  arguments:
    inputs: |
      hello
      how are you?
      what's your name?

- name: cmd
  script: echo hello
  expect-regex: hello

- name: bash
  bash: pwd
  expect: this should show a valid path
